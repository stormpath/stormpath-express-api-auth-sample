
IDSITE

try it with the production ID site beta feature

- it tests


MISC

// move authentication result to lib/authc (not in resource)

// start running unit and IT tests through `npm test` and the the coverage reporting tool

// allow a developer to selectively run just the unit tests or just the it tests

// start running IT tests on travis

// file tech debt issuie for "stop using authenticatio result"